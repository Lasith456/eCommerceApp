{
  "scripts": {
    "install:all": "npm-run-all install:admin install:frontend install:backend",
    "install:admin": "cd admin && npm install",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && npm install",

    "build:all": "npm-run-all build:admin build:frontend build:backend",
    "build:admin": "cd admin && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",

    "start:all": "concurrently \"cd admin && npm start\" \"cd frontend && npm start\" \"cd backend && npm start\""
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "concurrently": "^8.2.2"
  }
}
